language: python
python:
  - '3.5'
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-5
      - build-essential
      - cmake
      - libboost-dev
      - libboost-filesystem-dev

matrix:
  include:
    - env: BUILD_TYPE=deb DEBIAN_VERSION=stretch
    - env: BUILD_TYPE=deb DEBIAN_VERSION=buster
    - env: BUILD_TYPE=deb DEBIAN_VERSION=stretch CROSS=raspberrypi
    - env: BUILD_TYPE=python

env:
  - CXX=g++-5

install:
  - ci/scripts/${BUILD_TYPE}/install.sh

script:
  - ci/scripts/${BUILD_TYPE}/script.sh

after_success:
  - ci/scripts/${BUILD_TYPE}/after_success.sh

before_deploy:
  - cd ${TRAVIS_BUILD_DIR}

deploy:
- provider: pypi
  user: "${PYPI_USERNAME}"
  password: "${PYPI_PASSWORD}"
  on:
    tags: true
- provider: releases
  api_key: $GITHUB_OAUTH_TOKEN
  skip_cleanup: true
  file_glob: true
  file: 'src/build/dist/*'
  on:
    tags: true
